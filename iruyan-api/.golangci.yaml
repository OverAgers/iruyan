run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - gofmt
    - goimports
    - gocritic
    - errcheck
    - revive

issues:
  exclude-use-default: false

gofmt:
  simplify: true

revive:
  confidence: 0.8
  severity: warning
