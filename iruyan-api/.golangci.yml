run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - gofmt
    - goimports
    - errcheck
    - revive # コメント不足や命名のチェック（golintより柔軟）

gofmt:
  simplify: true

revive:
  confidence: 0.8
  severity: warning
