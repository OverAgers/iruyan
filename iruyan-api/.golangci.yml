version: 2

run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - revive

linters-settings:
  gofmt:
    simplify: true
  goimports:
    local-prefixes: github.com/OverAgers/iruyan

revive:
  confidence: 0.8
  severity: warning
